package design;

import java.awt.*;

public class Config {
    public static final int WIDTH = 10, HEIGHT = 25, SIZE = 20;
    public static final Color [] colors = {Color.BLACK, Color.RED, Color.GREEN};
    public static final Color background = Color.BLACK, FORE = Color.RED, LAND = Color.GREEN;
}
